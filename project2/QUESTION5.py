#How many MQTT subscribers receive a last will message derived from a subscription without a wildcard?
#filter: mqtt.willmsg && not (mqtt.willtopic contains "#" or mqtt.willtopic contains) non so se funziona
